{
  "name": "matthewderekrall-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio for Matthew Derek Rall (plain HTML/CSS/JS).",
  "scripts": {
    "minify:js": "terser js/translations.js -o js/translations.min.js --compress --mangle",
    "build": "npm run minify:js"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
